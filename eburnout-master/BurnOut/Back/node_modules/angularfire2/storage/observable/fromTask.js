import 'firebase/storage';
import { Observable } from 'rxjs/Observable';
export function fromTask(task) {
    return new Observable(function (subscriber) {
        task.on('state_changed', function (snap) { return subscriber.next(snap); }, function (e) { return subscriber.error(e); }, function () { return subscriber.complete(); });
        return { unsubscribe: task.cancel };
    });
}
//# sourceMappingURL=fromTask.js.map